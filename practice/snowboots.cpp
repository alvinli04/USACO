#include <bits/stdc++.h>
using namespace std;

int main(){
	//freopen("snowboots.in", "r", stdin);
	//freopen("snowboots.out", "w", stdout);
	
	int n,b; cin >> n >> b;
	int snow[n];
	for(int& i : snow) cin >> i;
	pair<int,int> boots[b];
	for(auto& i : boots){
		cin >> i.first >> i.second; //depth, step size
	}
	
	
}
