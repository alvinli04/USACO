#include <bits/stdc++.h>
using namespace std;

int N,M,K;

int* solve(int K, int *once, int *orig){
        int counter = 1;
        
}

int() main{
        ifstream in("swap.in");
        in >> N >> M >> K;
        int LR[M][2];
        for(int i = 0; i < M; i++){
                in >> LR[i][0] >> LR[i][1];
        }

}
